@model IEnumerable<TrainingAppMVC1.Models.TestModel>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.colOne)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.colTwo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.colThree)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.colOne)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.colTwo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.colThree)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
